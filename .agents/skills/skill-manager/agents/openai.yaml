interface:
  display_name: "Skill Manager"
  short_description: "Repo-specific patterns and workflows for skill-manager"
  default_prompt: "Use the skill-manager repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true